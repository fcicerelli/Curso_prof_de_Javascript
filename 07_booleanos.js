// Booleano true / false

// Ciclos / Condicionales

let notificaciones_activas = false;
let isActive = false;

let resultado = Boolean(1);  // true
let resultado2 = Boolean(0);  // false

// evaluan falso:
// undefined, Null, 0, NaN, "", false